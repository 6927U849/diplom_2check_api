package utils;

public class ThreadSleep {
    public static void run() throws InterruptedException {
        Thread.sleep(5000);
    }
}
//объявление метода run(), который является статическим (может быть вызван без создания экземпляра класса) и не возвращает значения

//для приостановки выполнения потока на заданное время
